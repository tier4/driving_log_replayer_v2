# Pinned for colcon/Autoware: pip install at configure time (see driving_log_replayer_v2/CMakeLists.txt)
# and for environments where OS packages are too old for Pydantic v2 / perception_eval.
# CMake: -r constraints-pip.txt, then pip install --ignore-installed -c … -r requirements.txt (apt/pip RECORD)
# constraints-pip.txt: setuptools>=70,<80 (colcon-core) and Py3.12 / ImpImporter-safe range.
pydantic>=2.5.1 # e.g. Ubuntu: python3-pydantic is still 1.x on 24.04; runtime needs v2
transforms3d==0.4.2 # e.g. Ubuntu: python3-transforms3d can be older
numpy<2 # If not specified, numpy 2 can be pulled by nuscenes-devkit
# nuscenes / evaluators (PyPI has wheels for common Linux + CPython; sdist may need libgeos-dev, etc.)
Shapely>=2.0.6
nuscenes-devkit>=1.1.9,<1.2.0
# driving_log_replayer_v2_analyzer (ROS) — same as pyproject.toml; not on rosdep
click>=8.1.7
plotly>=5.19.0
kaleido>=0.2.1,!=0.2.1.post1
