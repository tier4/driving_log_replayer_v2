# Stage 3: closed-loop シム実行のラン定義 (cloud / local 共通)。
# 既定は 4-way (sim_normal / sim_kus0020 / sim_perfect / sim_godot) 比較。
#
# vehicle_model のサフィックスで sim 種別を切り替える (scenario_test_runner.launch.py):
#   j6_gen2                 : DELAY_STEER_ACC_GEARED_WO_FALL_GUARD (j6_gen2_description の dynamics 既定)
#   j6_gen2_perfect_tracker : PERFECT_TRAJECTORY_TRACKER (planner 軌跡を理想追従。Godot binary 不要)
#   j6_gen2_godot           : EXTERNAL (Godot binary 必須。godot_executable で指定)
#
# Godot バイナリパスは cloud / local 共通で /opt/godot_autoware_simulator/ に統一。
#   cloud : .webauto-ci/main/autoware-setup/run.sh が v0.6.2 を /opt に配置 (自動)。
#   local : 同じ /opt に配置 or symlink する (詳細は README.ja.md)。
#
# params: simulator_model パラメータ上書き (cases.yaml の params と同型)。
#   step3_run_sims が simple_sensor_simulator.<key>:=<value> として launch に渡し、
#   description の simulator_model.param.yaml を上書きする (後勝ち)。
#   description パッケージを増やさずに dynamics 変種 (例 k_us) を作れる。

sim_runs:
  # j6_gen2_description の DELAY_STEER_ACC_GEARED_WO_FALL_GUARD (dynamics 既定値)
  - tag: sim_normal
    vehicle_model: j6_gen2
    architecture_type: awf/universe/20250130

  # j6_gen2 + アンダーステア係数 k_us=0.020 rad/(m/s^2)。
  # ω = vx·tan(δ)/(L + k_us·vx^2)。k_us=0 で理想 bicycle に一致。
  - tag: sim_kus0020
    vehicle_model: j6_gen2
    architecture_type: awf/universe/20250130
    params:
      k_us: 0.020

  # DELAY_STEER_ACC_GEARED_WO_FALL_GUARD の最善パラメータ調整版 (cases.yaml の best_normal と同一)。
  # **複数データセット・5 エリア横断**でロバスト同定: E2E 自律走行 20 データセット
  # (takanawa / odaiba / kawasaki / omika / shiojiri) の open-loop N=20 rollout 終端誤差を各データセットの
  # baseline 比に正規化 (分母は低ダイナミクス走行の暴発防止にフロアクリップ: yaw 0.12deg / pos 3cm) し、
  # dataset 横断の mean+worst を balance する目的関数で最小化して決定
  # (multi_dataset_tune.py + make local_multidataset_run)。
  #   旧 (単一データセット同定) 値 k_us=0.005 / steer_time_constant=0.30 / debug_steer_scaling_factor=0.90
  #   は 1 データセットに過適合していた。多エリア化で steer 側 proxy は汎化せず仕様値が最善、縦方向
  #   acc_time_constant も過大だと shiojiri の pos を悪化させるため引き下げ、k_us=0.018 (物理 understeer,
  #   実機直接推定 ~0.022 と整合) / acc_time_constant=0.30 / steer_time_constant=0.15 /
  #   debug_steer_scaling_factor=1.0 (=仕様値) に収束。
  #   正規化 yaw@N20: mean 0.636 / worst 0.898 (baseline=1.0)、正規化 pos@N20: mean 0.872 / worst 1.093。
  #   kus0020 (yaw mean 0.656 / worst 1.111 / pos mean 0.954) を yaw mean・worst と pos mean で上回る。
  # spec 値の steer_time_constant / debug_steer_scaling_factor も明示する (横断同定の結論を
  # closed-loop 経路でも暗黙の description 既定に依存させない)。
  - tag: best_normal
    vehicle_model: j6_gen2
    architecture_type: awf/universe/20250130
    params:
      k_us: 0.018                       # 物理 understeer (実機推定 ~0.022 と整合)
      steer_time_constant: 0.15         # = 仕様値 (steer 側 proxy は多エリアで汎化せず)
      debug_steer_scaling_factor: 1.0   # = 仕様値 (同上)
      acc_time_constant: 0.30           # 縦方向 (pos-mean 改善。0.45 超は shiojiri pos を悪化)

  # 理想軌跡追従 (planner 出力を完全追従)。dynamics 誤差ゼロの上限基準。
  - tag: sim_perfect
    vehicle_model: j6_gen2_perfect_tracker
    architecture_type: awf/universe/20250130

  # 外部 Godot シム。cloud / local 共通で /opt 配下の Godot バイナリを使用。
  - tag: sim_godot
    vehicle_model: j6_gen2_godot
    architecture_type: awf/universe/20250130
    godot_executable: /opt/godot_autoware_simulator/godot_autoware_simulator.x86_64
