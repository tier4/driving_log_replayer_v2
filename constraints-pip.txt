# Single source for pip/setuptools/wheel lower bounds (Python 3.12 / sdist build isolation).
# 1) Bootstrap:  pip install -U -r constraints-pip.txt
# 2) Main deps: pip install -c constraints-pip.txt -r requirements.txt
pip>=24.0.0
setuptools>=70.0.0
wheel>=0.42.0
